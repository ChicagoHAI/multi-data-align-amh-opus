[project]
name = "research-workspace"
version = "0.1.0"
description = "Research workspace for multimodal data alignment in adolescent mental health"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
